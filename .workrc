# shellcheck shell=bash
#
# source additional work configs and aliases
#._____________________________________________________________________________
#`

config=~/.config/"$(whoami)"/dotfiles

[ -f "$config/.workrc-$(whoami)" ] && . "$config/.workrc-$(whoami)"
[ -f "$config/.workrc-$(whoami)-aliases" ] && . "$config/.workrc-$(whoami)-aliases"
