#._____________________________________________________________________________
#`
# Ghostty
#
# - https://ghostty.org/
# - https://ghostty.org/docs/config
#
# Config originally generated by the Ghostty Config tool
#
# - https://ghostty.zerebos.com/
# - https://github.com/zerebos/ghostty-config
#
# On MacOS this file is synced to:
#
# - $HOME/.config/ghostty/config
#
# See `../../configurationalize.sh`
#._____________________________________________________________________________
#`

#.
# Fonts
#
# https://ghostty.org/docs/config/reference#font-family
# https://ghostty.org/docs/config/reference#font-size
#
font-family = Lilex Nerd Font Mono Medium
font-size = 18

#.
# Background
#
# https://ghostty.org/docs/config/reference#background-opacity
# https://ghostty.org/docs/config/reference#background-blur
#
background-opacity = 0.93
background-blur = 16

#.
# Theme + colors
#
# https://ghostty.org/docs/config/reference#theme
#
theme = Popping and Locking

# notes on other themes
#
#theme = Alien Blood
# pretty good, best green theme
# looks better in kitty so probably should check any customizations there
#
#theme = Box
# possibly too green?
# would need to update to better match: prompt, tmux, vim
#
#theme = Ciapre
# not terrible, blu-purp theme with yellow text
# would def need to update: prompt, tmux, vim
#
#theme = Cobalt Next Dark
# not bad, kind of like solarized dark
# would def need to update: prompt, tmux, vim
#
#theme = Cutie Pro
# not bad, like dracula (pastels) but with dark gray background
# would def need to update: prompt, tmux, vim (borders etc)
#
#theme = Darkmatrix
# not bad, dark blu-purp theme with army green text
# would def need to update: prompt, tmux, vim
#
#theme = Everblush
# not bad, dark blue ocean-y theme
# would need to update: prompt, tmux, vim
#
#theme = Gruvbox Dark Hard
# could def use this, dark blu-gray bg with yellowish text
# would need to update: prompt, tmux, vim
#
#theme = Hipster Green
# nah, not enough color variation
#
#theme = Kanagawa Dragon
# not bad, purple-y theme with yellow text
# would need to update: prompt, tmux, vim
#
#theme = Matrix
# good green fun but main color is too dark
# other than that would work ok with current prompt, tmux, vim
#
#theme = Neobones Dark
# pretty good, dark ocean-ish colors
# would need to update: tmux, vim (though `vim --clean` looks nice)
#
#theme = Popping and Locking
# not bad, rather like a witch or halloween theme
# would need to update: tmux, vim (`vim --clean` looks pretty decent)
#
#theme = Royal
# not bad, mostly purple theme
# would need to update: tmux, vim (borders etc)
#
#theme = Seafoam Pastel
# pretty fun, name is pretty accurate :)
# would need to update: prompt (a bit), tmux, vim (borders etc)
#
#theme = Solarized Dark Higher Contrast
# pretty much what you would expect from the name but, fresh! :)
# would need to update: prompt (a bit), vim (borders etc)
#
#theme = Zenwritten Dark
# pretty good, kind of like a dark zenburn (ish)
# would need to update: vim (borders etc)
#
#
# NOTE:
# below is how the ghostty config tool writes out the theme and colors
# which is handy if you want to tweak any individual colors
# (only need to set theme name if no tweaking required)
#
#theme = Alien Blood
#background = #0f1610
#foreground = #637d75
#selection-background = #1d4125
#selection-foreground = #73fa91
#cursor-color = #73fa91
#cursor-text = #0f1610
#palette = 0=#112616
#palette = 1=#7f2b27
#palette = 2=#2f7e25
#palette = 3=#717f24
#palette = 4=#2f6a7f
#palette = 5=#47587f
#palette = 6=#327f77
#palette = 7=#647d75
#palette = 8=#3c4812
#palette = 9=#e08009
#palette = 10=#18e000
#palette = 11=#bde000
#palette = 12=#00aae0
#palette = 13=#0058e0
#palette = 14=#00e0c4
#palette = 15=#73fa91
